{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // empty' | { read -r f; [[ \"$f\" =~ ^/Users/alastorwu/projects/revue/(src|public|bin|test)/ ]] && cd /Users/alastorwu/projects/revue && npm test; } 2>/dev/null || true",
            "timeout": 60,
            "statusMessage": "Running tests..."
          }
        ]
      }
    ]
  }
}
