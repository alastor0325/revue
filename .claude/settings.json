{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // empty' | bash -c '{ read -r f; root=$(git rev-parse --show-toplevel 2>/dev/null) || exit 0; [[ \"$f\" =~ ^\"$root\"/(src|public|bin|test)/ ]] && npm --prefix \"$root\" test; } 2>/dev/null || true'",
            "timeout": 60,
            "statusMessage": "Running tests..."
          }
        ]
      }
    ]
  }
}
